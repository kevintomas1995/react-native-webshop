class Product {
  constructor(id, ownerId, title, imageUrl, descrition, price) {
    this.id = id;
    this.ownerId = ownerId;
    this.title = title;
    this.imageUrl = imageUrl;
    this.descrition = descrition;
    this.price = price;
  }
}

export default Product;
